#include "Zebra.hpp"
#include "World.hpp"

namespace world {
/*

void Zebra::create_child(World& curWorld) {
    this->reprodCd_ = -1;
    if (this->sex_ == Sex::MALE) {
        return;
    }
    Zebra zebraChild;
    zebraChild.position_ = this->position_;
    curWorld.zebrasArray_.push_back(zebraChild);
} 
*/
}